ext {
    //sdk编译版本
    COMPILE_SDK_VERSION = 28
    //最小要求的sdk版本
    MIN_SDK_VERSION = 14
    //sdk目标版本
    TARGET_SDK_VERSION = 28

    //###################### 依赖库start ###########################

    // v7 包依赖库
    APP_COMPAT_V7_VERSION = "28.0.0"
    // 与属性布局的版本
    CONSTRAINT_LAYOUT_VERSION = "1.1.3"
    // lottie 动画版本
    LOTTIE_ANIMATION_VERSION = "2.3.1"
    // recyclerView 版本
    RECYCLERVIEW_VERSION = "28.0.0"

    //###################### 依赖库end ###########################
}